build:
  # TODO: Make this work?
  # symlink-install: true
  mixin:
    - build-testing-on
    - compile-commands
    - mold
    - rel-with-deb-info
    - ccache
  event-handlers:
    - console_direct+
